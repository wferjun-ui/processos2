<Window x:Class="SistemaJuridico.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Login" Height="400" Width="350"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    
    <Border Background="White" CornerRadius="10" Margin="10" Effect="{StaticResource MaterialDesignShadowDepth2}">
        <StackPanel Margin="30" VerticalAlignment="Center">
            <materialDesign:PackIcon Kind="ScaleBalance" Width="50" Height="50" HorizontalAlignment="Center" Foreground="{StaticResource PrimaryHueMidBrush}"/>
            <TextBlock Text="SISTEMA JURÍDICO" HorizontalAlignment="Center" FontWeight="Bold" FontSize="18" Margin="0,10,0,30" Foreground="#555"/>

            <TextBox Text="{Binding _username}" materialDesign:HintAssist.Hint="Usuário" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15"/>
            <PasswordBox x:Name="PassBox" materialDesign:HintAssist.Hint="Senha" Style="{StaticResource MaterialDesignOutlinedPasswordBox}" Margin="0,0,0,20"/>

            <Button Content="ENTRAR" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PassBox}" Height="45"/>
            <Button Content="SAIR" Command="{Binding FecharCommand}" Style="{StaticResource MaterialDesignFlatButton}" Foreground="Red" Margin="0,10,0,0"/>
        </StackPanel>
    </Border>
</Window>
